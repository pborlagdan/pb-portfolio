{
  "name": "paul-jason-portfolio",
  "version": "1.0.1",
  "description": "Full-stack developer portfolio — React (Vite) frontend + Node/Express backend.",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c magenta,cyan \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
